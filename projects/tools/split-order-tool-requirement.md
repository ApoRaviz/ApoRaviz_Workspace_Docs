# Split Order Tool Notes

## Where This Requirement Lives

The canonical product requirement for `split-order-txt` belongs in the project repository:

```txt
ApoRaviz_Tools/docs/split-order-txt-requirement.md
```

Reason:

- `ApoRaviz_Tools` stores product-specific requirements, commands, architecture, and implementation details.
- `ApoRaviz_Workspace_Docs` stores reusable learning, teaching notes, and case studies that help future projects.

## Reusable Lessons To Capture Later

- Node.js stream reader for large files
- Two-pass file processing
- CLI backup flow after successful processing
- CSV parsing edge cases such as quoted commas and escaped quotes
